# =============================================================================
# .dockerignore - Exclude files from Docker build context
# =============================================================================
# Purpose: Reduce build context size and prevent sensitive files from being
# copied into Docker images. Smaller context = faster builds.
# =============================================================================

# Dependencies (will be installed fresh in container)
node_modules/
node_modules_old/
.npm/
.pnp/
.pnp.js
.yarn/

# Next.js build outputs (will be generated during build)
.next/
out/
build/
.turbo/

# Testing
coverage/
.nyc_output/
*.test.ts
*.spec.ts
__tests__/

# Environment files (must be passed via docker-compose or --env-file)
.env
.env.local
.env*.local
.env.development
.env.test
.env.production.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*

# Operating System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*~

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*.swn
.project
.classpath
.settings/
*.sublime-workspace
*.sublime-project

# Version control
.git/
.gitignore
.gitattributes
.github/

# Docker files (don't need these inside the image)
Dockerfile*
docker-compose*.yml
.dockerignore

# CI/CD
.circleci/
.travis.yml
.gitlab-ci.yml
Jenkinsfile

# Documentation
README.md
LICENSE
CHANGELOG.md
*.md

# Vercel
.vercel/

# TypeScript (generated files)
*.tsbuildinfo
next-env.d.ts

# Clerk (local configuration)
.clerk/

# Prisma (migrations will run separately, generated client excluded)
migrations/
lib/generated/
lib/generated/client/

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.cache

# Security
*.pem
*.key
*.cert
.npmrc

# Misc
.eslintcache
.stylelintcache
